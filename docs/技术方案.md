# StarHub - 完整技术方案

> 目标:基于方案 A(Tauri 2 + Vue 3 + Rust + Go)实现一款"数据库 + SSH/SFTP + Docker + AI"一站式开发运维桌面工具
> 文档版本: v0.2 (2026-06-04)
> 更新记录:
> - v0.1: 初版,Sidecar 选用 Node.js
> - v0.2: **Sidecar 改为 Go**,数据库驱动表/技术选型/团队配置/附录同步更新
> - v0.18: PostgreSQL/Kafka/NSQ 落地,统一可观测仪表盘,增加 Docker Unix-Over-Nc-Sudo over SSH
> 性质: 研发参考文档(可直接作为 PRD 骨架)

---

## 目录

1. [产品定位与核心价值](#1-产品定位与核心价值)
2. [目标用户与场景](#2-目标用户与场景)
3. [完整功能列表](#3-完整功能列表)
4. [整体架构](#4-整体架构)
5. [技术选型](#5-技术选型)
6. [模块实现思路](#6-模块实现思路)
7. [数据模型与本地存储](#7-数据模型与本地存储)
8. [安全设计](#8-安全设计)
9. [性能与体验指标](#9-性能与体验指标)
10. [跨平台打包与发布](#10-跨平台打包与发布)
11. [开发路线图(MVP → 完整版)](#11-开发路线图)
12. [团队与周期估算](#12-团队与周期估算)
13. [风险与对策](#13-风险与对策)
14. [附录:技术对比与参考项目](#14-附录)

---

## 1. 产品定位与核心价值

### 1.1 一句话定位
**"All-in-One 开发运维桌面中枢"** — 把数据库客户端、SSH 终端、文件传输、Docker 管理、AI 助手,装进同一个原生桌面应用,解决"窗口来回切"的痛点。

### 1.2 核心价值主张
| 痛点 | 现状(碎片化) | 我们的方案 |
|---|---|---|
| 工具多 | Navicat + Xshell + Portainer + 资源管理器同时开 | 一个窗口搞定所有 |
| 切换累 | 操作 A 时被 B 打断,上下文丢失 | 同屏三栏(本地/远程/编辑)、命令广播 |
| 配置散 | 资产、密钥、SQL 脚本散落各处 | 统一资产中心,云端可选同步 |
| 学习成本高 | 每个工具一套快捷键、一套逻辑 | 一套统一交互范式 |
| 智能化弱 | 各工具无 AI 协同 | 自然语言描述 → 自动拆解 → 跨工具执行 |

### 1.3 对标产品
- **海外**: Termius (SSH) + TablePlus (DB) + Lens (K8s) + Cursor (AI 思路)
- **国内**: HexHub (直接对标) + JetBrains Gateway + 1Panel (Web 版)
- **差异化**: 重点放在"AI 跨工具编排"和"国产数据库支持"

---

## 2. 目标用户与场景

### 2.1 用户画像

| 角色 | 使用频率 | 核心诉求 |
|---|---|---|
| **后端开发** | 每天 4+ 小时 | 连服务器看日志、查数据库、查容器 |
| **DBA / 数据工程师** | 每天 6+ 小时 | 多类型数据库管理、SQL 调优、大数据导入导出 |
| **运维 / SRE** | 每天 4+ 小时 | SSH 多服务器、批量操作、Docker/K8s 排查 |
| **全栈 / 独立开发者** | 每天 2+ 小时 | 一站式开发环境、轻量部署管理 |
| **DevOps 团队 Lead** | 每天 1+ 小时 | 资产统一管理、权限分发、审计 |

### 2.2 典型场景

1. **日常开发**: 打开应用 → 选服务器 → 终端 + 文件双栏 → 改完代码直接看日志
2. **线上排障**: AI 对话输入"查 payment-api 最近报错" → 自动 SSH 拉日志 + 查相关数据库表 + 给出建议
3. **数据迁移**: 选源库/目标库 → 选表 → 差异对比 → 一键同步结构与数据
4. **服务器管理**: 资产树分组 → 批量执行命令(查版本/打标签/拉镜像) → 结果汇总
5. **AI 辅助排障**: 截个图/贴个错误 → AI 解读 → 生成修复命令 → 一键 SSH 执行

---

## 3. 完整功能列表

> **P0** = MVP 必做(3-4 月出活)
> **P1** = 完整版(6-8 月)
> **P2** = 增值(1 年+)
> **P3** = 探索性,看反馈

### 3.1 全局基础功能

| # | 功能 | 优先级 | 说明 |
|---|---|---|---|
| G-01 | 跨平台桌面应用(Win/Mac/Linux) | P0 | Tauri 2 打包 |
| G-02 | 多语言(中/英) | P0 | i18n |
| G-03 | 亮色/暗色双主题 | P0 | 系统跟随 + 手动切换 |
| G-04 | 多窗口/多标签 | P0 | 主窗口 + 浮动窗口(命令面板/文件预览) |
| G-05 | 资产中心(连接管理) | P0 | 树形分组、标签、收藏 |
| G-06 | 资产导入/导出 | P0 | JSON 格式,方便迁移 |
| G-07 | 云端资产同步(可选) | P1 | 端到端加密,自建/第三方 |
| G-08 | 快捷键体系 | P0 | 类 VSCode 风格 |
| G-09 | 命令面板(⌘K) | P1 | 全局命令搜索/执行 |
| G-10 | 自动更新 | P1 | Tauri Updater + 灰度 |
| G-11 | 系统托盘 | P0 | 快速连接/状态 |
| G-12 | 开机自启(可选) | P2 | |
| G-13 | 代理设置 | P0 | HTTP/SOCKS5,支持按连接覆盖 |
| G-14 | 通知中心 | P1 | 长任务完成/异常推送 |
| G-15 | 全局搜索 | P1 | 跨连接搜索命令/SQL/文件 |
| G-16 | 操作历史与审计日志 | P1 | 谁在何时执行了什么 |
| G-17 | 国际化输入法兼容 | P0 | 中文输入法在终端不卡顿(关键痛点) |
| G-18 | 屏幕录制/截图标注 | P3 | 排障分享(截图已在 v0.93.0 落地:AI 对话输入框区域截图 + 矩形标注,见 CHANGELOG) |

---

### 3.2 SSH 终端模块

#### 3.2.1 连接管理

| # | 功能 | 优先级 |
|---|---|---|
| SSH-01 | 新建连接(主机/端口/用户/密码/密钥) | P0 |
| SSH-02 | 密钥管理(生成/导入/多算法 Ed25519/RSA/ECDSA) | P0 |
| SSH-03 | 密码 + 密钥双因素 | P0 |
| SSH-04 | 主机指纹验证(known_hosts) | P0 |
| SSH-05 | 连接分组、标签、收藏 | P0 |
| SSH-06 | 连接复用(同一服务器多标签共享连接) | P1 |
| SSH-07 | SSH Config 文件导入(~/.ssh/config 解析) | P1 |
| SSH-08 | 历史连接快速重连 | P0 |
| SSH-09 | 心跳保活 + 断线重连 | P0 |
| SSH-10 | 带宽/连接数限制 | P2 |

#### 3.2.2 终端能力

| # | 功能 | 优先级 |
|---|---|---|
| SSH-11 | 终端渲染(xterm.js) | P0 |
| SSH-12 | ANSI 颜色 / 256色 / TrueColor | P0 |
| SSH-13 | 多标签终端(单连接多 tab) | P0 |
| SSH-14 | 终端分屏(垂直/水平/网格) | P1 |
| SSH-15 | PTY 分配与终端大小同步 | P0 |
| SSH-16 | 中文输入法不卡顿(关键) | P0 |
| SSH-17 | 选中复制 / 右键粘贴 | P0 |
| SSH-18 | 终端搜索(Ctrl+Shift+F) | P0 |
| SSH-19 | 终端回滚历史(可配置行数 1K-100K) | P0 |
| SSH-20 | 主题切换(Solarized/Monokai/Tomorrow 等) | P1 |
| SSH-21 | 字体设置(等宽字体、字号、连字) | P1 |
| SSH-22 | 终端录制(文本格式回放) | P2 |
| SSH-23 | 终端快照/导出 | P2 |

#### 3.2.3 高级功能

| # | 功能 | 优先级 |
|---|---|---|
| SSH-24 | 跳板机(一层/多层) | P0 |
| SSH-25 | SSH 隧道(本地/远程/动态 SOCKS5) | P0 |
| SSH-26 | X11 转发 | P2 |
| SSH-27 | 端口转发 | P1 |
| SSH-28 | 代理连接(SOCKS5/HTTP) | P0 |
| SSH-29 | 快捷指令(可自定义命令片段) | P0 |
| SSH-30 | 批量执行命令(多服务器并行) | P0 |
| SSH-31 | 批量执行结果汇总对比 | P1 |
| SSH-32 | sudo 提权 | P0 |
| SSH-33 | 命令历史(本地+服务器) | P0 |
| SSH-34 | 命令自动补全(基于历史) | P1 |
| SSH-35 | 危险命令拦截(rm -rf / 等) | P1 |
| SSH-36 | 实时状态面板(CPU/内存/网速/磁盘) | P0 |
| SSH-37 | 服务器分组 + 标签 | P0 |
| SSH-38 | 资产管理(服务器注册表) | P0 |
| SSH-39 | 服务器健康检查 | P1 |
| SSH-40 | Agent Forwarding | P1 |

---

### 3.3 SFTP / 文件传输模块

| # | 功能 | 优先级 |
|---|---|---|
| SFTP-01 | 三栏布局(本地/远程/编辑) | P0 |
| SFTP-02 | 双栏布局(本地/远程) | P0 |
| SFTP-03 | 拖拽上传/下载 | P0 |
| SFTP-04 | 多文件并发传输 | P0 |
| SFTP-05 | 断点续传(大文件) | P0 |
| SFTP-06 | 传输队列 + 进度条 | P0 |
| SFTP-07 | 后台传输(不阻塞 UI) | P0 |
| SFTP-08 | 传输限速 | P2 |
| SFTP-08a | 传输暂停/继续(块边界中断,断点续传) | P1 |
| SFTP-09 | 文件搜索(模糊) | P0 |
| SFTP-10 | 文件预览(代码/图片/视频/文档) | P0 |
| SFTP-11 | 内置代码编辑器(Monaco/CodeMirror) | P0 |
| SFTP-12 | 文件直接编辑保存(远程) | P0 |
| SFTP-13 | 文件权限修改(chmod/chown) | P0 |
| SFTP-14 | 文件/目录创建/删除/重命名 | P0 |
| SFTP-15 | 符号链接支持 | P1 |
| SFTP-16 | 压缩/解压(zip/tar.gz) | P1 |
| SFTP-17 | 跨服务器文件传输(中转) | P0 |
| SFTP-18 | ZMODEM 协议(rz/sz) | P0 |
| SFTP-19 | SCP 协议 | P0 |
| SFTP-20 | 文件比对/合并(可视化 diff) | P1 |
| SFTP-21 | 文件历史版本(本地缓存) | P3 |
| SFTP-22 | 大文件分片(>2GB) | P0 |
| SFTP-23 | 文件夹对比同步 | P1 |
| SFTP-24 | 收藏/书签目录 | P1 |
| SFTP-25 | 上传/下载冲突策略 | P0 |

---

### 3.4 数据库客户端模块

#### 3.4.1 支持的数据库

| # | 数据库 | 驱动 | 优先级 |
|---|---|---|---|
| DB-01 | MySQL / MariaDB | go-sql-driver/mysql | P0 |
| DB-02 | PostgreSQL | jackc/pgx/v5 | P0 |
| DB-03 | SQLite | modernc.org/sqlite(纯 Go) | P0 |
| DB-04 | Redis | redis/go-redis/v9 | P0 |
| DB-05 | ClickHouse | clickhouse-go/v2(官方) | P1 |
| DB-05A | Kafka | segmentio/kafka-go | P1 |
| DB-05B | NSQ | nsqd TCP + HTTP Stats API | P1 |
| DB-06 | SQL Server | microsoft/go-mssqldb(官方) | P1 |
| DB-07 | Oracle | oracledb | P1 |
| DB-08 | MongoDB | mongodb (NoSQL 模式) | P2 |
| DB-09 | 达梦 DM | ODBC/dm-thick | P1 |
| DB-10 | 人大金仓 KingbaseES | pg 兼容协议 | P1 |
| DB-11 | OceanBase | MySQL 兼容协议 | P1 |
| DB-12 | OpenGauss | pg 兼容协议 | P1 |
| DB-13 | 华为 GaussDB | 私有协议 | P2 |
| DB-14 | TiDB | MySQL 兼容 | P1 |
| DB-15 | DuckDB | duckdb-async | P3 |

#### 3.4.2 连接管理

| # | 功能 | 优先级 |
|---|---|---|
| DB-16 | 新建连接向导 | P0 |
| DB-17 | SSH 隧道连接(连内网 DB) | P0 |
| DB-18 | SSL/TLS 连接 | P0 |
| DB-19 | 连接池管理 | P0 |
| DB-20 | 多库切换(同连接多 database) | P0 |
| DB-21 | 只读模式 | P0 |
| DB-22 | 测试连接 | P0 |
| DB-23 | 连接超时配置 | P0 |

#### 3.4.3 SQL 编辑器

| # | 功能 | 优先级 |
|---|---|---|
| DB-24 | SQL 高亮(CodeMirror 6) | P0 |
| DB-25 | 自动补全(表名/字段名/关键字) | P0 |
| DB-26 | 代码格式化(sql-formatter) | P0 |
| DB-27 | 多 SQL 分号分割执行 | P0 |
| DB-28 | 选中执行 | P0 |
| DB-29 | 执行历史(可重放) | P0 |
| DB-30 | 多结果集展示 | P0 |
| DB-31 | 执行计划分析(EXPLAIN) | P0 |
| DB-32 | 查询性能分析(慢查询标记) | P1 |
| DB-33 | 参数化查询(防注入) | P0 |
| DB-34 | 代码片段(Snippet)管理 | P1 |
| DB-35 | 代码片段变量插值 | P1 |
| DB-36 | 事务包裹 | P0 |
| DB-37 | Lint(SQL 静态检查) | P2 |

#### 3.4.4 数据浏览与编辑

| # | 功能 | 优先级 |
|---|---|---|
| DB-38 | 表格视图(虚拟滚动百万级) | P0 |
| DB-39 | 网格视图(双视图) | P0 |
| DB-40 | 行内编辑(双击改单元格) | P0 |
| DB-41 | 新增/复制/删除行 | P0 |
| DB-42 | 撤销/重做 | P0 |
| DB-43 | 筛选/排序/分页 | P0 |
| DB-44 | 高级筛选(可视化) | P1 |
| DB-45 | 列宽记忆 | P0 |
| DB-46 | 修改/新增高亮着色 | P0 |
| DB-47 | JSON/二进制字段格式化 | P0 |
| DB-48 | BLOB 字段预览/下载 | P0 |
| DB-49 | 大字段懒加载(>1MB 截断) | P0 |
| DB-50 | 数据导出(CSV/JSON/SQL/Excel) | P0 |
| DB-51 | 数据导入(CSV/JSON/SQL/Excel) | P0 |
| DB-52 | 流式查询(避免大结果集 OOM) | P0 |
| DB-53 | 批量编辑(选中多行改字段) | P1 |
| DB-54 | 快捷键(⌘D 复制行等) | P1 |
| DB-55 | 数据字典导出 | P0 |
| DB-56 | 查找与替换 | P0 |

#### 3.4.5 表结构管理

| # | 功能 | 优先级 |
|---|---|---|
| DB-57 | 表/视图列表(树形) | P0 |
| DB-58 | 字段编辑(增删改) | P0 |
| DB-59 | 索引/主键/外键/唯一约束 | P0 |
| DB-60 | 类型推导(改类型兼容检查) | P0 |
| DB-61 | 字段注释编辑 | P0 |
| DB-62 | 表注释编辑 | P0 |
| DB-63 | DDL 预览(可视化) | P0 |
| DB-64 | 字段重排(拖拽) | P0 |
| DB-65 | 索引/约束可视化 | P0 |
| DB-66 | 表重命名/复制/清空/删除 | P0 |
| DB-67 | 视图编辑 | P1 |
| DB-68 | 存储过程/函数编辑 | P1 |
| DB-69 | 触发器编辑 | P2 |
| DB-70 | 字符集/排序规则 | P0 |
| DB-71 | 存储引擎(MyISAM/InnoDB) | P1 |

#### 3.4.6 数据库对象

| # | 功能 | 优先级 |
|---|---|---|
| DB-72 | 用户/权限管理 | P1 |
| DB-73 | 数据库创建/删除 | P0 |
| DB-74 | 备份/恢复(mysqldump/pg_dump) | P1 |
| DB-75 | 跨库结构对比(差异高亮) | P0 |
| DB-76 | 跨库结构同步(生成 ALTER 脚本) | P0 |
| DB-77 | 跨库数据传输 | P0 |
| DB-78 | 跨库复制粘贴(单元格级) | P0 |
| DB-79 | 数据库模糊搜索(全局搜表/字段) | P0 |
| DB-80 | DDL 版本管理(Git 式) | P1 |
| DB-81 | Schema 文档自动生成 | P1 |
| DB-82 | 慢日志分析 | P2 |
| DB-83 | Binlog/审计日志查看 | P2 |

---

### 3.5 Docker 容器管理模块

| # | 功能 | 优先级 |
|---|---|---|
| DOCKER-01 | 连接本地 Docker(unix socket/named pipe) | P0 |
| DOCKER-02 | 通过 SSH 连接远程 Docker | P0 |
| DOCKER-03 | 通过 TCP 连接远程 Docker | P1 |
| DOCKER-03A | 复用 SSH 资产连接 Docker Socket | P0 |
| DOCKER-03B | Unix-Over-Nc / Unix-Over-Nc-Sudo | P0 |
| DOCKER-04 | Docker Desktop 集成(Mac/Win) | P0 |
| DOCKER-05 | 容器列表(状态/资源/端口) | P0 |
| DOCKER-06 | 容器启动/停止/重启/删除 | P0 |
| DOCKER-07 | 容器创建向导(docker run → GUI) | P1 |
| DOCKER-08 | 容器交互终端(持久 TTY exec,复用 SSH xterm,原生提示符/历史/Tab 补全/交互程序,工作目录保持) | P0 |
| DOCKER-09 | 容器日志(实时/历史) | P0 |
| DOCKER-10 | 容器日志过滤(关键字/正则) | P0 |
| DOCKER-11 | ANSI 彩色日志渲染 | P0 |
| DOCKER-12 | 日志时间戳/行号标记 | P0 |
| DOCKER-13 | 双流日志(stdout/stderr 分离) | P0 |
| DOCKER-14 | 日志行数限制(1K-100K) | P0 |
| DOCKER-15 | 容器文件管理(浏览/上传/下载) | P0 |
| DOCKER-16 | 容器内编辑器 | P1 |
| DOCKER-17 | 镜像列表 | P0 |
| DOCKER-18 | 镜像拉取/推送 | P0 |
| DOCKER-19 | 镜像仓库加速(国内 mirror) | P0 |
| DOCKER-20 | 多镜像仓库源配置 | P0 |
| DOCKER-21 | 镜像导入/导出(save/load) | P0 |
| DOCKER-22 | 镜像历史/层查看 | P1 |
| DOCKER-23 | 镜像清理(悬空/未使用) | P1 |
| DOCKER-24 | 容器资源监控(CPU/内存/IO/网络) | P0 |
| DOCKER-25 | 实时趋势图(ECharts) | P0 |
| DOCKER-26 | 历史监控数据回溯 | P1 |
| DOCKER-27 | 进程级资源分析(进入容器看 top) | P0 |
| DOCKER-28 | 容器网络管理 | P1 |
| DOCKER-29 | 容器卷管理 | P1 |
| DOCKER-30 | Docker Compose 编辑/部署 | P1 |
| DOCKER-31 | 镜像构建(可视化 Dockerfile 编辑) | P2 |
| DOCKER-32 | 容器编排(K8s 桥接) | P3 |
| DOCKER-33 | Swarm 模式 | P3 |
| DOCKER-34 | 容器迁移(导出到另一主机) | P2 |

---

### 3.6 AI 助手模块(差异化亮点)

| # | 功能 | 优先级 |
|---|---|---|
| AI-01 | 对话式交互窗口 | P0 |
| AI-02 | 角色与资源引用(`@Agent` / `#LOCAL` / `#SSH` / `#DB` / `#Docker` / `#Excel`) | P0 |
| AI-03 | 多 LLM 支持(GPT/Claude/Gemini/通义/本地 Ollama) | P0 |
| AI-04 | API Key 管理(本地加密存储) | P0 |
| AI-05 | 自然语言 → 操作序列生成 | P0 |
| AI-06 | Function Calling(把 SSH/SFTP/SQL/Docker 操作封装为 tool) | P0 |
| AI-07 | 多步任务执行 + 进度回显 | P0 |
| AI-08 | 对话上下文(关联当前资源) | P0 |
| AI-09 | 历史会话管理(最近列表、恢复、单条删除) | P0 |
| AI-10 | 截图识别(粘贴图片 → AI 解读) | P1 |
| AI-11 | 错误日志智能解读 | P0 |
| AI-12 | SQL 自动生成(描述需求 → SQL) | P0 |
| AI-13 | 配置文件/代码解释 | P1 |
| AI-14 | 命令行参数建议 | P1 |
| AI-15 | AI 修复建议(给可执行的修复命令) | P0 |
| AI-16 | 一键回滚(AI 操作的命令可追溯) | P0 |
| AI-17 | 多模态(图片/语音输入) | P2 |
| AI-18 | 团队共享 Prompt 模板 | P2 |
| AI-19 | 私有知识库(RAG over 本地文档) | P2 |
| AI-20 | MCP 协议支持(stdio / Streamable HTTP / 兼容 SSE,挂载外部 tool) | P1 |
| AI-21 | 成本统计(每次对话的 token / 花费) | P1 |
| AI-22 | 流式输出(SSE) | P0 |
| AI-23 | 多 AI Agent(角色提示、独立会话、复制/删除) | P0 |
| AI-24 | Agent 级 Skill 绑定与多 Agent 协作提示 | P0 |
| AI-25 | Planner Agent 先规划、Execution Agent 按顺序或独立批次并行执行 | P0 |
| AI-26 | 计划问题点击选项、当前 Agent 与步骤状态可视化 | P0 |
| AI-27 | 资产级工作区引用(`#SSH-测试服务器`) | P0 |
| AI-28 | 自定义 Skill 创建与 JSON / Markdown / SKILL.md 导入 | P0 |
| AI-29 | 全局应用工具注册表(能力、资产、标签、设置、新建入口) | P0 |
| AI-30 | `#` 授权工作区无标签页直连执行(SSH / DB / Redis / ES / Docker / Excel) | P0 |
| AI-31 | Planner 自主创建一次性专职 Agent 与调度并行 Agent | P0 |
| AI-32 | `#LOCAL` 跨平台本机 Shell 与文件系统操作 | P0 |
| AI-33 | 发送区三步提问引导与排障/变更/SFTP/MCP 模板 | P0 |

---

### 3.7 协作与团队(增值)

| # | 功能 | 优先级 |
|---|---|---|
| COLLAB-01 | 团队工作空间(共享连接、密钥) | P2 |
| COLLAB-02 | 终端共享(多人同会话) | P3 |
| COLLAB-03 | 操作审计(谁执行了 rm -rf) | P1 |
| COLLAB-04 | 权限分级(管理员/普通成员/只读) | P2 |
| COLLAB-05 | 团队消息(类似 Slack) | P3 |
| COLLAB-06 | 审批流(危险操作需审批) | P2 |
| COLLAB-07 | 共享 SQL 片段库 | P1 |
| COLLAB-08 | 共享快捷指令库 | P1 |
| COLLAB-09 | 共享 AI Prompt 模板 | P2 |
| COLLAB-10 | 共享监控面板 | P3 |

---

### 3.8 监控与告警

| # | 功能 | 优先级 |
|---|---|---|
| MON-01 | 多服务器实时面板 | P0 |
| MON-02 | CPU/内存/磁盘/网络/IO 指标 | P0 |
| MON-03 | 进程列表(类似 top) | P0 |
| MON-04 | 磁盘挂载信息 | P0 |
| MON-05 | 趋势图(可配置时间范围) | P0 |
| MON-06 | 告警规则(阈值 + 持续时间) | P1 |
| MON-07 | 告警渠道(邮件/Webhook/飞书/钉钉/企微) | P1 |
| MON-08 | 告警静默期 | P2 |
| MON-09 | 自定义监控脚本 | P2 |
| MON-10 | 大盘模式(多服务器拼接) | P2 |

---

### 3.9 快捷指令与脚本

| # | 功能 | 优先级 |
|---|---|---|
| SN-01 | 快捷指令管理(增删改) | P0 |
| SN-02 | 变量插值({{host}} {{user}} {{date}}) | P0 |
| SN-03 | 分组与标签 | P0 |
| SN-04 | 导入/导出 | P0 |
| SN-05 | 一键发送到终端 | P0 |
| SN-06 | 一键批量执行(多服务器) | P0 |
| SN-07 | 条件分支(根据命令返回码) | P2 |
| SN-08 | 链式执行(管道) | P2 |
| SN-09 | 社区市场(下载别人的指令集) | P3 |

---

### 3.10 安全与合规

| # | 功能 | 优先级 |
|---|---|---|
| SEC-01 | 主密码(应用启动解锁) | P0 |
| SEC-02 | 系统 Keyring 集成(macOS Keychain/Windows Credential Vault/Linux Secret Service,Keyutils 兜底) | P0 |
| SEC-03 | 敏感字段加密(AES-256-GCM) | P0 |
| SEC-04 | 危险命令二次确认 | P0 |
| SEC-05 | 危险操作日志(可追溯) | P0 |
| SEC-06 | 连接密码永不上传(端到端加密同步) | P1 |
| SEC-07 | 指纹/面容解锁(移动端备用) | P3 |
| SEC-08 | 自动锁屏(空闲 N 分钟) | P1 |
| SEC-09 | IP 白名单 | P2 |
| SEC-10 | 完整审计日志(不可篡改) | P2 |

---

### 3.11 可扩展性

| # | 功能 | 优先级 |
|---|---|---|
| EXT-01 | 插件系统(自定义面板) | P2 |
| EXT-02 | 主题市场 | P2 |
| EXT-03 | 自定义协议(SDK 写新的连接器) | P3 |
| EXT-04 | Webhook 出站(完成后回调) | P1 |
| EXT-05 | CLI 模式(headless,自动化) | P2 |
| EXT-06 | REST API(给外部系统调用) | P3 |

---

### 3.12 移动端/平板(可选)

| # | 功能 | 优先级 |
|---|---|---|
| MOBILE-01 | iPad 适配 | P3 |
| MOBILE-02 | Android 平板 | P3 |
| MOBILE-03 | 触屏优化的终端键盘 | P3 |

---

## 4. 整体架构

### 4.1 分层架构(五层)

```
┌─────────────────────────────────────────────────────────────────┐
│  Layer 5: 表现层 (Presentation)                                  │
│  Vue 3 + Vuetify 3 / Element Plus + Pinia                      │
│  终端 xterm.js / 编辑器 Monaco+CodeMirror / 图表 ECharts       │
├─────────────────────────────────────────────────────────────────┤
│  Layer 4: 交互层 (Interaction) - Tauri Bridge                   │
│  Tauri IPC (invoke / event) + 双向事件流                        │
│  鉴权、权限申请、文件系统访问                                   │
├─────────────────────────────────────────────────────────────────┤
│  Layer 3: 业务逻辑层 (Business Logic) - Tauri Rust 进程        │
│  资源管理、连接池、协议适配、密钥管理                           │
│  SSH/SFTP/Docker/系统监控(用 Rust crate)                       │
├─────────────────────────────────────────────────────────────────┤
│  Layer 2: 数据服务层 (Data Service) - Go Sidecar               │
│  数据库驱动池(连接池/查询编排)                                │
│  流式数据桥(大结果集 → 前端)                                  │
│  导入导出引擎                                                  │
├─────────────────────────────────────────────────────────────────┤
│  Layer 1: 资源层 (Resources)                                    │
│  本地 SQLite(资产/配置) / 远程 SSH/DB/Docker / LLM API         │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 进程模型

```
┌─────────────────────────────────────────────────────────────┐
│  进程 1: Tauri 主进程 (Rust)                                │
│  ├── 系统集成 (WebView 窗口管理/托盘/通知)                  │
│  ├── IPC 路由                                                │
│  ├── 资源/连接池管理器                                       │
│  ├── SSH/SFTP/Docker 客户端(异步)                           │
│  ├── 跳板机/隧道服务                                         │
│  ├── 密钥保险箱(系统 Keyring)                               │
│  ├── 进程: Go Sidecar 启动器                                  │
│  └── 自动更新                                                │
├─────────────────────────────────────────────────────────────┤
│  进程 2: WebView (系统原生,主窗口)                          │
│  └── Vue 3 应用 + xterm.js + Monaco + CodeMirror            │
├─────────────────────────────────────────────────────────────┤
│  进程 3: Go Sidecar (Rust 启动,作为子进程)                   │
│  ├── 数据库驱动池(MySQL/PG/Redis/...)                        │
│  ├── 大数据流式处理                                          │
│  ├── 导入导出引擎                                            │
│  └── IPC 服务(通过 stdio / HTTP 跟主进程通信)              │
├─────────────────────────────────────────────────────────────┤
│  进程 N: 远程服务器 (用户资产)                              │
│  ├── SSH 守护进程 → SFTP 协议                                │
│  ├── 数据库服务端口                                          │
│  └── Docker Daemon (本地 socket 或 SSH 通道)                │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 数据流(以"SQL 查询"为例)

```
[用户输入 SELECT * FROM users WHERE ...]
        ↓
[Vue 组件: SqlEditor]
        ↓ invoke('db:query', {connId, sql})
[Tauri IPC Bridge]
        ↓ Rust 主进程接收,转发到 Go Sidecar
[Go: DatabaseAdapter.Query(ctx, connId, sql)]
        ↓
[database/sql + pgx.CopyFrom / ... → 流式获取数据]
        ↓ 分批推送(SSE / chunked)
[Tauri 端: 收数据 → 转发到前端事件 'db:result:chunk']
        ↓
[Vue: 表格组件累加数据 → 虚拟滚动渲染]
```

### 4.4 数据流(以"SSH 命令执行"为例)

```
[用户在终端敲入 ls -la]
        ↓ xterm.js onData
[Vue 组件 → invoke('ssh:write', {sessionId, data})]
        ↓
[Rust: SSH Session 通过 russh write data]
        ↓ PTY 分配
[远端 shell 执行 → stdout/stderr 实时流]
        ↓
[Rust: 通过 tokio channel 收数据]
        ↓ emit 'ssh:data'
[Vue: xterm.js write(bytes)]
```

---

## 5. 技术选型

### 5.1 桌面壳:Tauri 2

**为什么不用 Electron**:
- 体积:Tauri 20MB vs Electron 100MB+
- 内存:Tauri 50% 节省
- 性能:Rust 后端,无 Node 启动开销
- 安全:默认更严格的权限模型

**Tauri 2 核心能力**:
- 多窗口/多 WebView
- 系统托盘/通知
- 自动更新(集成 Tauri Store)
- 强类型 IPC(`#[tauri::command]` + `invoke<T>`)
- 插件生态(FS/Dialog/Shell/HTTP/Store/Security)

### 5.2 前端:Vue 3 + Vite

**核心库**:

| 类别 | 库 | 说明 |
|---|---|---|
| 框架 | Vue 3.4+ | Composition API + `<script setup>` |
| 构建 | Vite 5+ | HMR 秒级 |
| 路由 | Vue Router 4 | |
| 状态 | Pinia 2 | 配合持久化插件 |
| UI 库 | Vuetify 3 / Element Plus | 二选一,看风格 |
| 终端 | xterm.js 5+ | FitAddon / WebLinksAddon / SearchAddon |
| 图表 | ECharts 5+ | 监控趋势图 |
| 代码编辑 | Monaco Editor | 文件/JSON 大字段 |
| SQL 编辑 | CodeMirror 6 + lang-sql | 轻量、可定制 |
| 差异比对 | monaco-diff / diff-match-patch | 文件/SQL 差异 |
| 虚拟列表 | vue-virtual-scroller | 百万行表格 |
| 表单 | VeeValidate + Zod | 类型安全 |
| 国际化 | vue-i18n | 中/英 |
| HTTP | ofetch / axios | 内部 IPC 包装 |
| Markdown | marked + DOMPurify | AI 回复渲染 |

### 5.3 后端:Rust (主进程)

| 类别 | 库 | 说明 |
|---|---|---|
| 异步运行时 | tokio | 全异步基础 |
| SSH 客户端 | russh | 纯 Rust,支持现代 SSH |
| SFTP | russh-sftp | SFTP 子协议 |
| Docker | bollard | Docker API 客户端 |
| HTTP | reqwest | LLM API / Webhook |
| 数据库 | sqlx (可选) | 本地 SQLite 持久化 |
| 序列化 | serde + serde_json | 配置/协议 |
| 加密 | aes-gcm / argon2 | 敏感数据加密 |
| 系统监控 | sysinfo | CPU/内存/进程 |
| 网络 | tokio::net | 端口转发/隧道 |
| 配置 | config-rs | 加载/合并 |
| 错误处理 | thiserror + anyhow | |
| 日志 | tracing + tracing-subscriber | |
| 密钥存储 | keyring-rs | 系统 Keyring |

### 5.4 后端:Go (Sidecar)

**为什么用 Go 而不是 Node.js**:
- 单二进制,Sidecar 启动快(< 50ms)、体积小(10-20MB)
- 静态类型 + goroutine,数据库连接池/流式查询天然适合
- 跨平台交叉编译简单(`GOOS=windows GOARCH=amd64 go build` 一行)
- DB 驱动生态跟 Node 五五开,PG/Redis 反而更强
- 跟 Rust 一样编译型,后端技术栈统一,运维心智一致

| 类别 | 包 | 说明 |
|---|---|---|
| 运行时 | Go 1.22+ | 静态二进制 |
| 进程通信 | stdio JSON-RPC | 跟 Rust 主进程通信 |
| MySQL | `github.com/go-sql-driver/mysql` | 官方推荐,标准 `database/sql` 集成 |
| PostgreSQL | `github.com/jackc/pgx/v5` | 性能之王,原生协议,流式一等公民 |
| SQLite | `modernc.org/sqlite` | 纯 Go,无 CGO,跨平台编译无坑 |
| Redis | `github.com/redis/go-redis/v9` | 官方维护,cluster/sentinel 全支持 |
| ClickHouse | `github.com/ClickHouse/clickhouse-go/v2` | 官方驱动 |
| SQL Server | `github.com/microsoft/go-mssqldb` | 微软官方 |
| Oracle | `github.com/sijms/go-ora` | 纯 Go,无需 Oracle Instant Client |
| MongoDB | `go.mongodb.org/mongo-driver` | 官方驱动 |
| 国产库 | `github.com/alexbrainman/odbc` + 系统 ODBC | 达梦/金仓通用兜底 |
| SQL 工具 | `github.com/jmoiron/sqlx` | Struct 映射 + 命名参数 |
| 数据处理 | `encoding/csv` + `encoding/json` + `goccy/go-json` | 导入导出 |
| 验证 | `github.com/go-playground/validator/v10` | struct tag 校验 |
| 日志 | `github.com/rs/zerolog` / `slog`(标准库) | 高性能 JSON 日志 |
| 配置 | `github.com/spf13/viper` | 多源配置 |
| 指标 | `github.com/prometheus/client_golang` | metrics |
| 追踪 | `go.opentelemetry.io/otel` | 链路追踪 |
| 测试 | `github.com/stretchr/testify` | 断言库 |
| Mock | `github.com/golang/mock` + `sqlmock` | 单元测试 |

**为什么选 modernc.org/sqlite 而不是 mattn/go-sqlite3**:
- modernc.org/sqlite 是 **纯 Go 实现**(把 SQLite C 代码翻译成 Go),**无 CGO**
- CGO 跨平台编译是噩梦(macOS/Linux/Windows 需要不同 gcc),纯 Go 一个 `go build` 搞定
- 性能跟 CGO 版本持平,启动略慢但运行时一致
- 唯一缺点:二进制大 ~2MB(自带 SQLite 源码),可接受

**Rust ↔ Go 通信协议**:
- MVP 阶段:`stdio JSON-RPC`(Go `bufio.Scanner` 读 stdin,简单稳定)
- 性能敏感场景:升级到 `gRPC over Unix Socket`(两侧都有成熟实现)
- 数据格式:`JSON`(MVP) / `Protobuf`(高吞吐优化)

### 5.5 数据存储

| 数据 | 存储 | 加密 |
|---|---|---|
| 资产/连接 | 本地 SQLite (sqlx) | 否 |
| 密钥 | 系统 Keyring (keyring-rs) | 系统保护 |
| 密码(备选) | 本地 SQLite + AES-256-GCM | 主密码派生 |
| 设置 | 本地 SQLite / Tauri Store | 否 |
| 缓存(Schema/Snippet) | 本地 SQLite | 否 |
| 传输队列 | 内存 + 持久化进度 | 否 |

### 5.6 LLM 集成

| 场景 | 推荐 |
|---|---|
| 云端首选 | Claude (Anthropic SDK) |
| 国内合规 | 通义千问 / DeepSeek / 文心 |
| 本地/隐私 | Ollama (llama3.1, qwen2.5) |
| 统一接口 | 自建 LLM Gateway(OpenAI 兼容) |

**协议**: OpenAI 兼容 API,统一抽象 Provider

### 5.7 CI/CD & 发布

- CI: GitHub Actions(免费、跨平台 runner)
- 桌面打包: Tauri CLI + electron-builder 风格
- 自动更新: Tauri Updater + 自建更新服务(S3/Cloudflare R2)
- 渠道: stable / beta / nightly
- 签名: Windows 代码签名 + macOS Notarization

---

## 6. 模块实现思路

### 6.1 SSH 模块(Rust + russh)

**核心数据结构**:
```rust
struct SshSession {
    id: SessionId,
    handle: russh::client::Handle<Client>,
    pty_tx: mpsc::Sender<Bytes>,        // 输入通道
    output_rx: broadcast::Receiver<Bytes>, // 输出通道
    metadata: SshMetadata,
}

struct Client { /* 认证处理 */ }
impl russh::client::Handler for Client { ... }
```

**关键流程**:
1. **认证**: 密码 / 私钥(读 ~/.ssh/id_ed25519 并解析) / Agent forwarding
2. **通道打开**: `channel_open_session` + `request_pty`(列宽行数从 xterm 同步)+ `exec_command` 或 `shell`
3. **数据流**: 
   - 输入: `data` 通道 → `channel.make_writer()`
   - 输出: `channel.wait()` → emit 事件
4. **断线检测**: keepalive_interval + 超时
5. **重连**: 保存 last_session_id,失败后自动 reconnect

**难点**:
- 中文输入法: xterm.js 用 `modes` API 报告输入模式,后端不能简单按字节流转发
- ZMODEM: PTY 输出保持 `Vec<u8>` 原始字节并由 Webview 侧 `zmodem.js` Sentry 检测;协议回包走 `ssh_write_binary`,普通终端输出再以流式 `TextDecoder` 写入 xterm
- 跳板机链: 用 `tokio::net::TcpStream` 中转,或 russh 的 channel 直接转发

### 6.2 SFTP 模块

**实现**: russh-sftp 协议 / ssh2 crate(更成熟但同步)

**核心数据结构**:
```rust
struct TransferTask {
    id: TaskId,
    source: Endpoint,    // local / ssh://user@host/path
    target: Endpoint,
    bytes_total: u64,
    bytes_done: u64,
    speed: u64,          // bytes/s
    state: TransferState,
}
```

**关键能力**:
- **并发**: tokio::spawn 多任务并行
- **超时**: 每条 SSH 资产保存 `sftpTimeoutSec`，默认 30 秒、允许 5–300 秒；Rust 同时用于 SFTP 子系统通道打开和 `russh-sftp` 单次请求超时
- **启动策略**: 每条 SSH 资产保存 `sftpLaunchMode`：`auto` 先请求标准 `sftp` subsystem，失败后自动诊断常见服务端路径并仅在找到可执行程序时直接启动；`subsystem` 禁止降级；`custom` 使用用户配置的远端 Unix 绝对路径
- **真实诊断**: SFTP 建链必须等待 SSH channel request 的 `Success/Failure`，并在协议初始化期间保留远端 stderr、exit status、exit signal 与提前关闭状态；禁止把已获得的远端错误覆盖成通用 Timeout
- **可控降级**: 自动诊断只执行固定探测脚本，依次检查 OpenSSH 常见路径及 `command -v sftp-server`；自定义路径经过绝对路径、长度和控制字符校验后再做 POSIX 安全引用，不接受任意 shell 命令
- **服务端修复优先**: 推荐在 `sshd_config` 使用 `Subsystem sftp internal-sftp`；外部二进制路径异常时，客户端降级用于兼容和应急，不替代服务端配置修复
- **断点续传**: SFTP `open` + offset + append
- **跨服务器**: 中转模式(本地内存/disk 缓存)
- **大文件**: 分片(4MB/片)+ 进度事件

### 6.3 数据库模块(主要在 Go Sidecar)

**架构图**:
```
[Rust: 收到 invoke('db:query')]
    ↓
[查连接池拿 *sql.DB / *pgx.Conn]
    ↓
[启动 goroutine,执行查询]
    ↓
[rows.Next() 逐行迭代]
    ↓
[json.Marshal → 写 stdout(分片,每 1K 行一批)]
    ↓
[Rust: 读 stdout chunk → emit 'db:result:chunk']
    ↓
[前端: 累加数据 → 虚拟滚动渲染]
```

**连接池设计**:
```go
type ConnectionPool struct {
    mu    sync.RWMutex
    pools map[string]*sql.DB  // connId -> 标准库连接池
    pgxs  map[string]*pgxpool.Pool  // PG 专用 pgx 池
}

func (p *ConnectionPool) Acquire(ctx context.Context, connId string) (*sql.DB, error) {
    p.mu.RLock()
    defer p.mu.RUnlock()
    db, ok := p.pools[connId]
    if !ok {
        return nil, fmt.Errorf("connection not found: %s", connId)
    }
    if err := db.PingContext(ctx); err != nil {
        return nil, err
    }
    return db, nil
}
```

**PG 流式查询(pgx 一等公民)**:
```go
func (a *PgAdapter) QueryStream(ctx context.Context, connId, sql string) (<-chan Row, <-chan error) {
    out := make(chan Row, 1024)
    errCh := make(chan error, 1)
    go func() {
        defer close(out)
        rows, err := a.pgxs[connId].Query(ctx, sql)
        if err != nil { errCh <- err; return }
        defer rows.Close()
        for rows.Next() {
            row, _ := rowsToMap(rows)
            select {
            case out <- row:
            case <-ctx.Done():
                return
            }
        }
        errCh <- rows.Err()
    }()
    return out, errCh
}
```

**MySQL 流式查询(标准 database/sql)**:
```go
func (a *MysqlAdapter) QueryStream(ctx context.Context, connId, sql string) (<-chan Row, <-chan error) {
    out := make(chan Row, 1024)
    errCh := make(chan error, 1)
    go func() {
        defer close(out)
        rows, err := a.db.QueryContext(ctx, sql)
        if err != nil { errCh <- err; return }
        defer rows.Close()
        cols, _ := rows.Columns()
        for rows.Next() {
            row := scanRow(rows, cols)
            select {
            case out <- row:
            case <-ctx.Done():
                return
            }
        }
        errCh <- rows.Err()
    }()
    return out, errCh
}
```

**Redis 适配**:
```go
// 走 RESP 协议
func (a *RedisAdapter) Execute(ctx context.Context, connId, cmd string, args ...interface{}) (any, error) {
    return a.clients[connId].Do(ctx, cmd, args...).Result()
}
```
- Key 浏览器的普通搜索词自动转换为 Redis glob `*关键词*`;用户显式输入 `*`、`?`、`[]` 时保留原始高级 Pattern
- 选中具体 DB 或刷新/搜索后,前端基于游标连续调用 `SCAN`,直到游标归零;搜索作用于完整 DB,不只过滤首屏已加载 Key
- 连续扫描使用请求 token 丢弃切换 DB、快速改关键词产生的过期响应,避免旧结果覆盖新检索

**导入导出**:
- 导出:`rows.Next()` → `encoding/csv` / `encoding/json` Writer → 流式写盘
- 导入:解析 CSV/Excel(`github.com/xuri/excelize/v2`)→ `INSERT` 批量事务(每批 1000 行)
- 大表:用 `COPY` (PG) / `LOAD DATA INFILE` (MySQL) 走协议级快速路径

**关系型数据库数据网格**:
- MySQL / ClickHouse 的表数据、SQL 编辑器结果和独立查询结果统一由 `DbSimpleGrid.vue` 使用原生 HTML 表格和虚拟滚动渲染;`DataGrid.vue` 继续负责服务端分页、筛选、排序、导出、刷新和批量保存
- 数据列名与类型写入 sticky 表头,列宽根据列定义与前 40 行样本自适应且支持拖拽调整;数字、布尔、JSON、长文本与 `NULL` 保持独立语义和样式
- 有主键的表数据允许直接在 DOM 单元格编辑,改动先进入 dirty 集合,再通过 `Ctrl/Cmd+S` 或保存按钮批量生成主键 `WHERE` 并调用更新 RPC;无主键或普通 SQL 结果设置为只读
- 点击列头或列工具栏触发原有服务端/客户端排序;列选择器继续提供服务端字段筛选,分页与导出协议不受渲染引擎替换影响
- 虚拟窗口按固定行高、滚动位置和 overscan 只渲染可见数据行;字段元数据用于列头悬停备注和结构详情

**Excel 工作簿编辑**:已于 v0.80.0 退役。数据库导入和导出仍通过 Sidecar 的 `excelize` 能力处理 XLSX/CSV 文件,但 StarHub 不再提供独立工作簿资产、路由或前端编辑器。

**统一可观测性与元数据安全**:
- MySQL/PostgreSQL/Redis/SSH/Docker/Kafka/NSQ 指标卡采集真实状态并保留最近 20 个样本,用折线图或环图展示趋势;详情表展示连接 IP、用户、等待事件、容器端口、具体慢 SQL/当前 SQL。
- MySQL `TABLE_ROWS`、ClickHouse `total_rows` 等服务端允许为 NULL 的统计字段必须在 SQL 投影中 `COALESCE`;业务上确实可空的默认值、索引统计映射到 Go 指针,禁止扫描进 `int64/string` 标量。
- 产品入口统一使用 `ProductIcon.vue` 与官方品牌 SVG;无法从上游图标库取得的产品使用明确产品字标,不得借用其他数据库图标。

**Docker SSH Socket 传输**:
- 用户可在 Docker 连接中选择已有 SSH 资产及其跳板机配置;Sidecar 只接受 Rust trusted-host 存储返回的公钥,未知 Host Key 必须先通过 SSH 页面确认。
- `Unix-Over-Nc` 在远端执行 `nc -U <socket>`;`Unix-Over-Nc-Sudo` 执行 `sudo -n nc -U <socket>`,要求目标账号配置无交互 sudo 权限,避免密码提示卡死 HTTP 通道。

**Docker 容器交互终端**:
- 人工操作使用 `docker.execSessionStart/Read/Write/Resize/Close` 建立持久 TTY 会话,以原始字节双向传输并同步 xterm 的行列数;Sidecar 为每个会话保留有界输出缓冲并用长轮询唤醒前端。
- 会话在容器内依次选择 `bash`、`ash`、`sh` 的交互模式,提示符、历史、补全、信号键和全屏交互程序都由容器 Shell/TTY 原生处理。AI 与自动化仍使用会自行结束的一次性 `docker.exec`,避免交互会话占用工具调用。
**国产数据库适配**:
- 优先用兼容协议(DM/KingbaseES/OceanBase/OpenGauss 多兼容 PG/MySQL)
- 不兼容的用 ODBC 桥接(`github.com/alexbrainman/odbc` + 系统 ODBC 驱动)
- 连接串模板化,支持各厂商方言

**Sidecar 进程模型**:
```go
// Go 侧:启动后从 stdin 读 JSON-RPC,处理后写 stdout
func main() {
    log.SetOutput(os.Stderr) // 日志走 stderr,不污染 stdout 协议
    
    stdin := bufio.NewScanner(os.Stdin)
    stdin.Buffer(make([]byte, 1<<20), 10<<20) // 10MB buffer
    
    rpc := NewRpcServer()
    rpc.Register("db.query", handleQuery)
    rpc.Register("db.stream", handleStream)
    rpc.Register("db.exec", handleExec)
    // ...
    
    for stdin.Scan() {
        req := RpcRequest{}
        if err := json.Unmarshal(stdin.Bytes(), &req); err != nil {
            writeError(err)
            continue
        }
        go rpc.Handle(req) // 响应携带 request id,允许乱序返回
    }
}
```

**为什么 Go 比 Node 适合 Sidecar**:
- 单二进制 ~15MB,Node.js 运行时 + npm 驱动 ~80MB
- 启动 < 50ms,Node 200-500ms
- goroutine 处理并发连接池 / 流式天然适合,Node 单线程事件循环在 CPU 密集场景吃亏
- 跨平台编译简单(`GOOS=...` 一行),Node 需要考虑 node-gyp、原生模块

Rust 主进程将 stdin 写入与 stdout 读取拆为独立任务,使用请求 ID 映射 `oneshot` 响应通道。多个数据库请求可同时在途,默认 RPC 超时为 120 秒;Sidecar 退出时会一次性失败所有待处理请求,避免界面永久等待。启动阶段通过 `version` RPC 校验协议版本及关键方法清单,旧二进制或能力缺失会直接阻止应用启动并给出重装提示。
- 静态类型在数据库 schema 映射时编译期就能发现错误

### 6.4 Docker 模块

**连接方式**:
- **本地**:
  - Linux: `unix:///var/run/docker.sock`
  - Mac: 同上(Docker Desktop 转发)
  - Windows: `npipe:////./pipe/docker_engine`
- **远程**:
  - TCP: `tcp://host:2375`(无 TLS)/ `tcp://host:2376`(TLS)
  - SSH 通道(推荐): 通过 SshSession 打开 `exec "docker"` 通道,所有 docker API 通过 stdin/stdout 走 SSH 协议

**实现**: bollard crate,全异步

**镜像加速**:
- 用户配置 mirror(registry-mirrors)
- 客户端拦截 pull 请求,重写 registry 域名为 mirror
- 支持私有仓库凭证

### 6.5 AI 编排

**架构**: AI Gateway(本地) → LLM Provider

```
[Vue Chat UI]
    ↓ invoke('ai:chat', {messages, context})
[Rust: AI Gateway]
    ↓ 1. 加载系统 prompt + 工具定义
    ↓ 2. 调用 LLM(流式)
    ↓ 3. 收到 tool_call → 路由到具体 command
    ↓    (如 'ssh_exec' → 调用 SSH 模块)
    ↓ 4. 把工具结果再喂给 LLM
    ↓ 5. 循环直到 LLM 不再调用 tool
[Stream SSE → 前端]
```

**独立 AI 工作区(v0.19.0 → v0.29.0)**:
- 资产树增加 AI 一级类型,Agent 定义持久化在 Pinia `ai-v2` 状态中;Provider、API Key、模型、全局 Skill 与命令白名单仍复用唯一 `AiSettings`,不为每个 Agent 复制敏感配置。
- `@Agent` 把当前请求路由到对应角色提示;一次提及多个 Agent 时,第一个作为主角色、其余作为协作角色,各自绑定的 Skill 一并注入系统提示。
- v0.20.0 起每轮请求先进入 `Planner Agent`;v0.25.0 进一步允许 Planner 为专职步骤创建不持久化的临时 Agent,并把互不依赖、无共享写状态的连续步骤编成并行批次。写操作、前后依赖和共享目标强制顺序执行;执行结果、步骤状态、临时/并行标识与当前 Agent 都写回会话。v0.28.9 起 Planner 额外接收最近 24000 字符的有界多轮上下文,顺序 Executor 注入此前已完成步骤的有界结果,并在重试/重新规划时删除一次重复的当前请求。
- v0.28.9 起 `#SSH` / `#DB` / `#Docker` / `#Excel` / `#LOCAL` / `#本机` 为当前对话内的粘性工作区上下文。模块级引用在绑定时立即固化为当时已经存在的具体资产 ID,后续新增资产不会自动进入范围;输入区持续展示“已继承”徽标并提供清除入口,新对话与应用重启都撤销工具绑定。精确引用仍只暴露对应资产。
- v0.25.0 起全局工具注册表不再通过“打开资产 / 切换标签”间接执行任务。StarHub AI 根据当前会话 `#` 绑定构造无标签页直连运行时,把 `workspace` 参数硬路由到绑定资产;SSH / DB / Redis / Elasticsearch / Docker / Excel 工具在后台建立短生命周期连接并在步骤结束后释放,未绑定资产不会进入工具候选集。
- v0.26.0 增加独立的 `#LOCAL` / `#本机` 上下文。绑定后执行器先用 `local_system_info` 获取 `OS / arch / cwd / home / shell`,再按 Windows PowerShell 或 macOS/Linux POSIX `/bin/sh` 生成非交互命令;当前会话未绑定本机时所有 `local_*` tool 都不会注册给模型。写操作、白名单未命中与高危动作仍逐次确认;文件正文读取为只读、免确认(正文仍会发送给当前 AI Provider)。
- v0.28.9 起正式 tab 会话按 instanceId 在 `ai-sessions-v1` 中保存最近 30 个会话,每个会话最多 60 条、120000 字符的用户/助手文本;工具参数、工具输出、执行计划和 `#` 工具绑定不落盘。最近对话摘要保存同一 instanceId,可重新挂载原会话。流式请求在插入 UI assistant 占位前复制消息快照,避免空 assistant 污染 Provider 请求。
- v0.28.10 起 AI 工作区在 keep-alive 停用时记录消息容器的滚动位置和是否贴近底部,激活后恢复阅读位置;贴底会话继续跟随新内容,回看历史时不被流式更新强制拉回底部。
- v0.29.0 起资产树最近对话入口最多显示 10 条且不再依赖 LLM 当前配置状态;恢复继续复用原 instanceId,单条删除经过确认后同时清理摘要、运行时会话与 `ai-sessions-v1` 本地历史。发送区增加“引导”面板,用 Agent、`#` 工作区、目标/限制/验收三步解释请求结构,模板只填入草稿、不自动发送。
- v0.29.0 起 SSH 工作区 AI 与独立 AI 直连运行时共同注册 `sftp_list`、`sftp_stat`、`sftp_upload`、`sftp_download`。目录/元数据读取复用当前认证连接;上传读取本机路径和下载写入本机目录都强制逐次确认,单次限制 20 个显式路径。传输沿用 `TransferManager` 的流式队列并等待 done/failed/cancelled 后返回,避免 Executor 结束时提前断开临时 SSH 连接;SSH disconnect 同步注销 TransferManager 的 SFTP 句柄。
- v0.29.0 起 `AiSettings` 可配置多个 MCP Server。Rust MCP 客户端实现 2025-06-18 生命周期(`initialize` → `notifications/initialized`)、分页 `tools/list` 和 `tools/call`:stdio 子进程严格使用逐行 JSON-RPC 且 stdout 只接收协议消息;Streamable HTTP 接受 JSON / SSE 响应、维护 `Mcp-Session-Id` 并在 404/405 时兼容旧 HTTP+SSE;显式 SSE 先解析 endpoint 事件并限制同源。发现的工具以 `mcp__<server>__<tool>` 安全名称动态注册给 Executor,每次调用均进入全局人工确认区。stdio 环境变量和 HTTP Header 值写入系统 Keyring,Pinia 只持久化名称与空值占位。
- 本机 IPC 由 `src-tauri/src/commands/local.rs` 实现,覆盖目录列表、路径元数据、分段文本读取、文本写入、目录创建、文件复制、路径移动/重命名、删除和 Shell。Shell 超时限制为 1–120 秒,stdout/stderr 各截断到 512 KiB;文本单次读取上限 1 MiB、写入上限 2 MiB、目录单次最多 500 项,避免模型请求造成无界资源消耗。
- 自定义 Skill 既可在设置中创建,也可从 JSON、Markdown 或 `SKILL.md` 导入;导入阶段限制 256 KB,校验名称、prompt/instructions、资产作用域并去重,保存后可绑定到任意 Agent。
- 无标签页直连仍复用现有 Function Calling 安全门:只读命令命中白名单后直接执行,白名单未命中、强制确认、风险命令、ES 写入、Excel 写入和本机文件写操作会在全局计划卡中生成可点击的批准 / 拒绝按钮;允许加入白名单的命令额外提供“批准并加入白名单”。本机文件正文读取为只读操作、免确认,但内容会发送给当前 AI Provider。高危规则优先于白名单,并覆盖 Unix、PowerShell、CMD 与 macOS 管理命令。首次 SSH 主机指纹也必须在全局确认卡中人工确认。
- Planner 的信息缺口统一输出 2–4 个结构化互斥选项,界面直接渲染为按钮并把点击结果送回重新规划;Planner 和 Executor 都禁止要求用户输入 `A/B/C` 或序号完成选择。

**SSH AI 非交互边界(v0.20.0)**:
- `ssh_exec(_confirmed)` 只接受能自行结束的完整命令。`cat > file`、不完整 heredoc、交互编辑器/分页器、`tail -f` / `watch` 等在人工确认前就会被拒绝;写文件必须使用完整 heredoc 或 `printf`。
- 命令完成判定以「完成哨兵」为主通道(v0.47.2 起):AI 命令后追加独立一行 `printf '\033]777;starhub;ai-done;<ID>;<退出码>\007' "$?"`(自定义 OSC 序列,xterm 不渲染),在 PTY 原始字节流精确匹配收口并取退出码;哨兵被吞(引号未闭合 / csh / PowerShell 无 printf)时退回"执行前实际 prompt + shell prompt 模式"双通道识别,不再把 2 秒输出静默误判为完成。安全超时、用户停止和新会话会向共享 PTY 发送 Ctrl+C 恢复 shell。
- v0.28.10 起全局 AI 的无标签页 SSH 运行时调用 `ssh_connect_exec`,只建立认证连接并由 `ssh_exec` 按命令打开短生命周期 channel,不申请 PTY、不启动登录 shell,步骤结束后立即断开。手动终端和连接工作区内的交互 AI 仍走 `ssh_connect` + PTY;建链日志分别记录认证耗时、总耗时与连接模式。

**Tool 定义示例**:
```typescript
const tools = [
  {
    name: 'ssh_exec',
    description: '在远程 SSH 服务器上执行命令',
    parameters: {
      connection: { type: 'string', description: '连接名' },
      command: { type: 'string', description: 'shell 命令' }
    }
  },
  // db_query, docker_list, file_read, etc.
]
```

**安全**:
- 危险 tool 需用户确认(`require_user_confirm: true`)
- 每次执行可审计、可回滚
- 私有部署: LLM API 不允许外发(企业版)

### 6.5.1 AI 浏览器(无痕独立窗口,v0.103.0)

**形态**: AI 经 Function Calling 操作的浏览器是独立 Tauri 窗口
(`ai-browser`,`incognito(true)`),不是主窗口内的面板——AI 不占用主界面,
用户全程可见;无痕三平台语义:Windows WebView2 InPrivate / macOS WKWebView
nonPersistent DataStore / Linux WebKitGTK ephemeral WebContext(wry 0.55 原生)。

**控制通道(双层,自动降级)**:
- Windows 增强:`ICoreWebView2.CallDevToolsProtocolMethod`(CDP)——
  `Runtime.evaluate`(awaitPromise+returnByValue)、`Page.captureScreenshot`、
  `Input.dispatchMouseEvent/insertText/dispatchKeyEvent` 可信输入
- 跨平台兜底:`webview.eval()` 注入助手脚本(编号元素 DOM 序列化,含
  Shadow DOM / 同源 iframe 递归),结果经 `browser_internal_result` 命令
  oneshot 回传(wry eval 无返回值)
- 截图:Windows CDP / macOS `WKWebView.takeSnapshot`(objc2 薄封装)/
  Linux `webkit_web_view_get_snapshot`(webkit2gtk =2.0.2 与 wry 锁版)

**工具**(Rust 主进程内执行,`harness/tools.rs` BROWSER_TOOLS):
`browser_open/navigate/back/forward/reload/state/extract/click/type/press_key/
select_option/scroll/screenshot/eval` 共 14 个;观察类放行、动作类软确认、
`browser_eval` 恒确认(approval-bridge hard 档)。

**安全边界**: `capabilities/browser.json` 只给 `ai-browser` 窗口(任意
http/https origin)放行 `browser_internal_result` 一条命令;导航协议白名单
http/https/about:blank(拒绝 javascript:/file:/data:);无痕 profile 不落 Cookie。

**关窗联动**: 主窗口 CloseRequested 时销毁其余全部窗口
(detach-*/ai-browser/screenshot-overlay),杜绝孤儿窗口;ai-browser 销毁时
在途 eval 立即失败收口。

### 6.5.2 沙箱桌面(Ubuntu 容器沙箱平台,v0.105.0)

**形态**: E2B 式架构(模板 → 秒级实例 → 用完即焚),执行层复用现有 Docker
能力——AI 在一次性 Ubuntu 24.04 桌面容器(Xvfb + Xfce + x11vnc + noVNC +
websockify)里操作任意 Linux 桌面应用,noVNC 6080 端口映射回环,画面对用户
全程直播(「直播/接管」按钮开独立 Tauri 窗口全页加载 noVNC,v0.106.0 起;
此前为面板内嵌 iframe,尺寸受限且 permissions-policy 禁全屏)。完整设计:
`docs/superpowers/specs/2026-08-28-desktop-automation-design.md`。

**编排路径**: Rust `desktop/` 模块(`desktop/mod.rs` + `recipe.rs`)经 sidecar
Docker 适配器(M0 补齐的 createContainer/buildImage/copyTo/From/pause/commit/
createNetwork)驱动,无需自定义 guest agent;箱内桌面原语 = xdotool(键鼠/
窗口)/ scrot(截图)/ wmctrl(窗口列表)。

**模板与实例**: 模板 = 配方 TOML(base/memory/cpus/网络档/resolution/install/
provision,Rust 侧校验)+ 镜像 tag;实例/回放帧存 SQLite
(sandbox_templates / sandbox_instances / sandbox_replay_frames)。
`desktop_commit_sandbox` 把运行中实例固化为新模板(登录态/已装软件随镜像
沉淀)。配方注意:Ubuntu 24.04 的 firefox/chromium deb 是 snap 壳,容器内
不可用,默认配方装 mousepad,浏览器用 epiphany 或 Mozilla PPA。

**安全模型(设计 §5)**:
- 任务级授权:`desktop_create_sandbox` 的一次确认 = 本会话对该沙箱 60 分钟
  内全部箱内操作权限;授权存在性/过期/实例匹配由 Rust 宿主在执行点强制
  (审批层只决定 ask 级别,`desktop_exec` 恒确认 hard 档)
- 容器硬化:cap-drop ALL + no-new-privileges + 不挂 docker.sock;网络档
  none / restricted(自定义隔离网络)/ full
- 接管互斥:用户接管(`desktop_ui_open_live_window` takeover=true)期间 AI
  写操作一律拒绝,接管不撤销授权;直播窗口销毁由 Rust 自动释放接管
  (v0.106.0 起接管生命周期并入开窗命令,原 `desktop_set_takeover` 移除)
- 回放留档:每次写操作前自动截屏(sandbox_replay_frames);
  `desktop_type` 文本不进审计(审计摘要只记长度)

**人机协作**: `desktop_request_user_action` 广播
`starhub://desktop-user-action` 事件,前端常驻横幅(倒计时 + 「打开直播画面」
一键拉起接管窗口 + 「已完成」/「无法完成」)经 `desktop_user_action_reply`
应答;扫码登录/输密码等场景由此闭环。

**平台选择器**: 设置 → 沙箱平台 选择既有 Docker 连接(settings 表
`desktop.platform_asset_id`),默认本机(sidecar `docker.connect {}` 空参
client.FromEnv);实例落库时记录 platform,后续操作钉在该实例自己的平台上,
用户中途改设置不影响存量沙箱;所选连接失效时报错,绝不静默回落本机。

### 6.6 前端关键组件

**终端组件**(`TerminalPane.vue`):
- 包装 xterm.js,处理 IPC
- 输入法兼容: 监听 `keydown` 而非纯 onData
- 主题/字体/连字配置

**数据表格**(`DataTable.vue`):
- vue-virtual-scroller 虚拟滚动
- 单元格编辑: contenteditable + blur 提交
- 快捷键: 上下左右导航,Enter 编辑,Esc 取消
- 差异高亮: 修改行/单元格着色

**SQL 编辑器**(`SqlEditor.vue`):
- CodeMirror 6,自定义 lang-sql extension
- 自动补全: 从 schema 缓存拉表/字段
- 多光标、列选择、代码折叠

**资产树**(`AssetTree.vue`):
- 拖拽排序
- 右键菜单(连接/复制/删除/标签)
- 搜索过滤

---

## 7. 数据模型与本地存储

### 7.1 本地 SQLite Schema(简化)

```sql
-- 资产分组
CREATE TABLE asset_groups (
  id INTEGER PRIMARY KEY,
  name TEXT NOT NULL,
  parent_id INTEGER,
  icon TEXT,
  sort_order INTEGER,
  created_at INTEGER
);

-- 资产(连接)
CREATE TABLE assets (
  id TEXT PRIMARY KEY,         -- UUID
  type TEXT NOT NULL,           -- ssh / db / docker / redis
  name TEXT NOT NULL,
  group_id INTEGER,
  config_json TEXT NOT NULL,    -- 序列化配置
  key_id TEXT,                  -- 关联密钥表
  tags TEXT,                    -- JSON 数组
  favorite INTEGER DEFAULT 0,
  last_used_at INTEGER,
  created_at INTEGER,
  updated_at INTEGER
);

-- 密钥
CREATE TABLE keys (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  type TEXT NOT NULL,           -- password / private_key
  encrypted_data BLOB,          -- 加密后的密钥
  keyring_ref TEXT,             -- 系统 Keyring 引用
  fingerprint TEXT,
  created_at INTEGER
);

-- 快捷指令
CREATE TABLE snippets (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  group_id INTEGER,
  command TEXT NOT NULL,
  description TEXT,
  variables TEXT,               -- JSON
  scope TEXT,                   -- ssh / db / global
  created_at INTEGER
);

-- SQL 历史
CREATE TABLE sql_history (
  id INTEGER PRIMARY KEY,
  conn_id TEXT,
  sql TEXT,
  executed_at INTEGER,
  duration_ms INTEGER,
  rows_affected INTEGER,
  success INTEGER
);

-- 监控告警规则
CREATE TABLE monitor_rules (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  target_type TEXT,             -- ssh / docker
  target_id TEXT,
  metric TEXT,                  -- cpu / mem / disk
  operator TEXT,                -- > / < / ==
  threshold REAL,
  duration_sec INTEGER,
  channels TEXT,                -- JSON: webhook/email/...
  enabled INTEGER DEFAULT 1
);

-- 通用设置
CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value TEXT,
  updated_at INTEGER
);
```

### 7.2 加密策略

| 数据 | 加密方式 |
|---|---|
| 私钥 | 系统 Keyring |
| 密码 | 系统 Keyring |
| AI API Key | 系统 Keyring |
| 资产配置 | SQLite 仅存非敏感字段与 Keyring 引用 |
| 同步上传 | 端到端加密(libsodium) |
| 本地数据库文件 | 可选 SQLCipher |

Windows Credential Manager 的 credential blob 对 UTF-16 编码长度有限制。资产密钥超过安全阈值时使用带版本清单的分代分片存储;写入新分片成功后再原子切换清单,读取兼容旧版单条 JSON,更新/删除时清理旧分片。Linux 优先写入桌面 Secret Service 以跨重启持久化,仅在无桌面密钥环的 headless/WSL 环境回退到会话级内核 Keyutils。私钥正文仍只存在系统 Keyring,不会回落到 SQLite 或普通文件。

---

## 8. 安全设计

### 8.1 信任边界

```
┌────────────────────────────────────────────┐
│  不可信边界: 远程服务器、LLM API           │
│  - SSH 主机指纹验证                        │
│  - 数据库 SSL/TLS                          │
│  - Docker TLS                              │
└────────────────────────────────────────────┘
            ↕ 加密通道
┌────────────────────────────────────────────┐
│  半可信边界: WebView 渲染进程              │
│  - 严格 CSP                                │
│  - Tauri 权限白名单(只 expose 必要 API)   │
│  - DOMPurify 防 XSS                        │
└────────────────────────────────────────────┘
            ↕ IPC(参数校验)
┌────────────────────────────────────────────┐
│  可信边界: Rust 主进程 / Go Sidecar        │
│  - 所有 invoke 参数 zod 校验               │
│  - 危险命令二次确认                        │
│  - 审计日志                                │
└────────────────────────────────────────────┘
```

### 8.2 关键安全措施

1. **Tauri 权限最小化**: `tauri.conf.json` 中只注册必要的 plugin,command 显式声明
2. **CSP**: `default-src 'self'`,禁止 inline script
3. **输入校验**: 所有 IPC 参数 zod schema 校验
4. **密钥保护**: 密码、私钥与 AI API Key 不落盘明文,只存 Keyring 引用;长私钥按 UTF-16 安全预算分片并保留旧格式读取兼容
5. **危险拦截**: 终端侧检测 `rm -rf /`、`dd if=`、`mkfs` 等
6. **审计**: 所有"写"操作记入审计日志,带时间戳/用户/目标
7. **更新安全**: Tauri Updater 验证签名

---

## 9. 性能与体验指标

| 指标 | 目标 |
|---|---|
| 应用冷启动 | < 2s |
| 连接建立(SSH) | < 1.5s |
| SQL 查询响应(< 10K 行) | < 200ms |
| 大数据表格(100 万行) 滚动 | 60fps |
| 文件传输速度 | 跑满带宽(> 800Mbps 千兆环境) |
| 内存占用(空闲) | < 200MB |
| 安装包大小 | DEB/RPM/Windows < 35MB;自包含 WebKitGTK 的 AppImage < 120MB |
| 终端输入延迟 | < 30ms |
| 主题切换 | < 100ms |
| 自动更新下载(增量) | < 5s |

---

## 10. 跨平台打包与发布

### 10.1 平台支持

| 平台 | 最低版本 | 打包格式 |
|---|---|---|
| Windows | Win 10 (1809+) | .msi 安装版 / 便携 .exe |
| macOS | macOS 11 Big Sur+ | .dmg(arm64 + x86_64) |
| Linux | glibc 2.39+（Ubuntu 24.04 / Debian 13 / Fedora 40+） | .AppImage / .deb / .rpm（x86_64 + ARM64） |
| Linux 兼容版 | glibc 2.35+（Ubuntu 22.04 / Debian 12 / Fedora 38+） | .deb / .rpm（x86_64，无截图，`-ubuntu2204` 后缀） |

Linux 默认发布固定在 Ubuntu 24.04 原生 x86_64 / ARM64 runner 构建。AppImage 随包携带 WebKitGTK、GTK 与静态 Go sidecar,作为主流 glibc 桌面发行版的通用包;DEB/RPM 通过包元数据声明系统运行库。每个产物必须通过 `scripts/verify-linux-bundles.sh`,确认架构、sidecar 可执行权限与静态链接、包依赖以及主程序 `ldd` 无 `not found`;Alpine(musl)和无 FHS 兼容层的 NixOS 不在直接兼容范围。**截图功能依赖系统 PipeWire ≥ 1.0(Ubuntu 24.04 及以上)**:截图栈 xcap → pipewire/libspa 0.10.1 需要新版 spa 头,Ubuntu 22.04(0.3.48)连绑定都无法生成,旧系统上点击截图由主进程预检后提示升级,不做静默失败。

为兼容 Ubuntu 22.04 等旧桌面发行版,截图栈整体 gate 到 Cargo `screenshot` 特性(默认开启):`linux-legacy-2204.yml` 在 `ubuntu-22.04` 原生 runner 上以 `--no-default-features`(关截图)构建,不引入 xcap/pipewire 链,产物 glibc 下限回到 2.35,文件名带 `-ubuntu2204` 后缀随 tag 附到同一 Release;代价是该兼容版不含内置 AI「区域截图」,前端按钮点击归并为友好提示。

### 10.2 自动更新

- 自建更新服务: Cloudflare R2 + Workers(签名 + 元数据)
- Tauri Updater 配置:
  ```json
  {
    "plugins": {
      "updater": {
        "endpoints": ["https://update.example.com/{{target}}/{{arch}}/{{current_version}}"],
        "pubkey": "..."
      }
    }
  }
  ```
- 渠道: stable / beta / nightly
- 用户可关闭自动更新

### 10.3 代码签名

- **Windows**: EV 代码签名证书(SmartScreen 友好)
- **macOS**: Developer ID + Notarization(避免 Gatekeeper 拦截)
- **Linux**: GPG 签名 .deb/.rpm

---

## 11. 开发路线图

### Phase 1: MVP (Month 1-4)

**目标**: 跑通核心流程,验证架构可行性

**功能范围**:
- ✅ G-01 ~ G-06, G-08, G-11, G-13, G-17(基础)
- ✅ SSH-01 ~ SSH-09, SSH-11 ~ SSH-15, SSH-17, SSH-19, SSH-24 ~ SSH-28
- ✅ SFTP-01 ~ SFTP-14, SFTP-17, SFTP-22, SFTP-25
- ✅ DB-01 ~ DB-04, DB-16 ~ DB-78(主流程)
- ✅ DOCKER-01 ~ DOCKER-09, DOCKER-15, DOCKER-17, DOCKER-18, DOCKER-24
- ✅ SEC-01 ~ SEC-05
- ✅ AI-01 ~ AI-08, AI-15, AI-16, AI-22

**里程碑**:
- M1 (Month 1): 项目脚手架 + SSH 终端跑通
- M2 (Month 2): SFTP + 资产中心 + 跨平台打包
- M3 (Month 3): MySQL 完整支持 + Docker
- M4 (Month 4): AI 基础集成 + 内测发布

### Phase 2: 完整版 (Month 5-8)

- 所有 P0 收尾
- P1 功能(多 LLM、Compose、告警等)
- 完善监控、批量操作、协作基础
- 公测

### Phase 3: 商业化 (Month 9-12)

- P2 协作、安全、扩展
- 团队版特性
- 销售渠道(官网、企微、JetBrains 工具集等)

### Phase 4: 生态 (Year 2+)

- 插件市场
- 移动端
- 私有化部署方案

---

## 12. 团队与周期估算

### 12.1 MVP 最小团队(3 人,4 个月)

| 角色 | 数量 | 职责 |
|---|---|---|
| Rust 全栈 | 1 | Tauri + SSH + 系统集成 |
| Vue 全栈 | 1 | 前端 + 终端 + SQL 编辑器 |
| Go 全栈 | 0.5 | 数据库驱动(可兼产品) |
| 产品/设计 | 0.5 | 原型、UI、文档 |

### 12.2 完整版团队(6-8 人,8 个月)

| 角色 | 数量 |
|---|---|
| Rust 后端工程师 | 2 |
| Go 后端工程师 | 1 |
| Vue 前端工程师 | 2 |
| 设计 | 0.5 |
| 测试 | 1 |
| 产品 | 0.5 |
| 运维/发布 | 0.5 |

### 12.3 成本估算(以 MVP 算,自建团队)

- 工资(MVP 阶段 3 人 × 4 月 × 平均 3 万/月): 36 万
- 服务器/域名/CDN: < 1 万
- 代码签名证书: 0.5-1 万 / 年
- 杂项: 2 万
- **总计**: 约 40-50 万(不含创始人时间)

如使用 1-2 名兼职/外包,成本可压到 20-30 万。

---

## 13. 风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| 中文输入法在终端卡顿 | 高 | xterm.js modes API + 自定义键盘事件,持续测试 |
| ZMODEM 没有 Rust 实现 | 中 | 桥接 C 库(lrzsz),或先 Web 端用纯 JS 库 |
| 国产数据库兼容性 | 中 | 优先用兼容协议,ODBC 兜底,分批适配 |
| 跨平台打包踩坑 | 中 | Tauri 2 已大幅改善,提前预留 1 月调 |
| LLM 调用成本失控 | 中 | 本地缓存常用回答,token 限速,UI 显式显示花费 |
| ssh2/russh 性能瓶颈 | 低 | 实测再决定,99% 场景够用 |
| 团队人才稀缺(懂 Rust + 懂网络) | 中 | 优先招 Rust 通用人才,SSH 由 1 人专攻 |
| 数据安全合规 | 高 | 端到端加密 + 详细审计 + 第三方安全审计 |
| 竞品跟进(原 HexHub 闭源化) | 低 | 走开源路线反而是护城河 |
| Electron 替代方案(部分用户偏好) | 低 | 保留 Web 版作为长期备选 |

---

## 14. 附录

### 14.1 关键开源参考

| 项目 | 用途 | 借鉴点 |
|---|---|---|
| HexHub (https://github.com/EdikKing/hexhub) | 直接对标 | 产品形态、UI 范式 |
| Tabby (https://github.com/Eugeny/tabby) | 跨平台终端 | 插件系统、配置导入 |
| electerm (https://github.com/electerm/electerm) | SSH/SFTP 客户端 | UI 范式 |
| TablePlus | 数据库客户端 | 表格编辑体验 |
| DBeaver | 数据库客户端 | 数据库元数据管理 |
| lazydocker | Docker TUI | Docker 操作模型 |
| ImHex | 十六进制编辑器 | UI 风格参考 |
| 1Panel | Web 运维平台 | 现代化运维面板 |
| JumpServer | 堡垒机 | 安全/审计模型 |
| Termius | SSH 客户端 | 商业化产品形态 |

### 14.2 关键 Rust Crate

| Crate | 用途 |
|---|---|
| `russh` | SSH 客户端/服务端 |
| `russh-sftp` | SFTP 子协议 |
| `bollard` | Docker API |
| `tokio` | 异步运行时 |
| `tauri` | 桌面框架 |
| `serde` | 序列化 |
| `keyring` | 系统密钥管理 |
| `sysinfo` | 系统信息 |
| `tracing` | 日志 |
| `anyhow` / `thiserror` | 错误处理 |
| `aes-gcm` | 加密 |
| `argon2` | 密码哈希 |
| `reqwest` | HTTP 客户端 |

### 14.3 关键 Go 模块(Sidecar)

| 包 | 用途 |
|---|---|
| `github.com/go-sql-driver/mysql` | MySQL/MariaDB |
| `github.com/jackc/pgx/v5` | PostgreSQL(性能之王) |
| `modernc.org/sqlite` | SQLite(纯 Go,无 CGO) |
| `github.com/redis/go-redis/v9` | Redis |
| `github.com/ClickHouse/clickhouse-go/v2` | ClickHouse |
| `github.com/microsoft/go-mssqldb` | SQL Server |
| `github.com/sijms/go-ora` | Oracle(纯 Go,无需 Instant Client) |
| `go.mongodb.org/mongo-driver` | MongoDB |
| `github.com/alexbrainman/odbc` | ODBC 桥(国产库兜底) |
| `github.com/jmoiron/sqlx` | SQL 工具包(struct 映射) |
| `github.com/rs/zerolog` 或 `log/slog` | 日志 |
| `github.com/spf13/viper` | 配置 |
| `github.com/go-playground/validator/v10` | struct 校验 |
| `github.com/prometheus/client_golang` | 指标 |
| `go.opentelemetry.io/otel` | 链路追踪 |
| `github.com/xuri/excelize/v2` | Excel 读写(导入导出) |

### 14.3.b 关键 npm 包(前端)

| 包 | 用途 |
|---|---|
| `xterm` + `xterm-addon-*` | 终端 |
| `@codemirror/*` | SQL 编辑 |
| `monaco-editor` | 文件编辑 |
| `zod` | 校验 |
| `sql-formatter` | SQL 格式化 |
| `vue-virtual-scroller` | 虚拟列表 |
| `echarts` | 图表 |

### 14.4 推荐阅读

- Tauri 2 官方文档: https://tauri.app/
- russh 项目: https://github.com/Eugeny/russh
- bollard 项目: https://github.com/fussybeaver/bollard
- "Designing Data-Intensive Applications" - 数据流设计参考

---

## 文档结束

> 后续步骤:
> 1. 基于本方案搭建项目脚手架(可使用 `create-tauri-app`)
> 2. 跑通 SSH 终端的最小链路(关键路径)
> 3. 同步拆解 P0 任务,排迭代
> 4. 边做边验证假设(尤其是中文输入法和 LLM 工具调用)
